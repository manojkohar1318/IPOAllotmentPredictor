import { ContactPage } from '@/components/ContactPage';

export const metadata = {
  title: "Contact Us — NEPSE IPO Allotment Predictor",
};

export default function ContactUsPage() {
  return <ContactPage lang="EN" isDark={true} />;
}
