import { AboutSection } from '@/components/AboutSection';

export const metadata = {
  title: "About — Nepal's #1 IPO Allotment Predictor Tool",
};

export default function AboutPage() {
  return <AboutSection lang="EN" isDark={true} />;
}
